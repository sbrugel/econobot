export enum ItemType {
    Collectible = `Collectible`, // i.e. collectibles
    Message = `Message`, // displays a custom message when /viewitems is run
    Role = `Role` // contains property for name and color
}